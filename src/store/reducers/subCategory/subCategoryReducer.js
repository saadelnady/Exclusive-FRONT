const initialState = {
  isLoading: false,
  error: null,
  subCategories: [],
  subCategory: {},
  message: "",
};

const subCategoryReducer = (state = initialState, action) => {
  return state;
};

export { subCategoryReducer };
