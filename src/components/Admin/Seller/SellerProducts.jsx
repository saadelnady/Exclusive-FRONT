import React from "react";
import { OptionButton } from "../shared/OptionButton";

export const SellerProductsActions = ({ id }) => {
  return (
    <div>
      <OptionButton id={id} />
    </div>
  );
};
